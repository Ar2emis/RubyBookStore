pre-commit:
    parallel: true
    commands:
      rubocop:
        glob: "*.rb"
        run: bundle exec rubocop
    DatabaseConsistency:
      enabled: true
      quiet: false
      command: ['bundle', 'exec', 'database_consistency']
